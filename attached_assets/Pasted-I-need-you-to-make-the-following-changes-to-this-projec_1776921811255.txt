I need you to make the following changes to this project. Please implement all of them in order.

---

CHANGE 1: Update server/business-config.ts
Replace the entire file content with:

export const businessConfig = {
  name: "Selam CPA",
  legalName: "Selam Tax Inc.",
  dba: "Selam CPA",
  owner: "Yacob Tewelde, CPA, FCCA",
  phone: "301-640-8549",
  phoneE164: "+13016408549",
  email: "info@selamcpa.com",
  address: {
    street: "8593 Light Moon Way",
    city: "Laurel",
    state: "MD",
    zip: "20723",
    full: "8593 Light Moon Way, Laurel, MD 20723",
  },
  geography: {
    primary: "Laurel, MD",
    region: "DMV — DC, Maryland, Virginia",
    national: "All 50 states (virtual)",
    seoLocations: [
      "Laurel MD", "Columbia MD", "Baltimore MD",
      "Washington DC", "Northern Virginia",
      "Bethesda MD", "Silver Spring MD", "Rockville MD",
      "Arlington VA", "Alexandria VA"
    ],
  },
  calendly: "https://calendly.com/yber2001/30min",
  website: "https://selamcpa.com",
  social: {
    linkedin: "",
    facebook: "",
  },
  services: [
    "Tax Preparation & Planning",
    "Bookkeeping & Accounting",
    "Fractional CFO Services",
    "Audit, Review & Compilation",
    "AI Consulting for Accounting Firms",
    "Tax Strategy & Advisory",
  ],
  industries: [
    "Healthcare & Medical Practices",
    "Legal & Professional Services",
    "Real Estate & Property Management",
    "Technology & SaaS",
    "Retail & E-Commerce",
    "Construction & Contractors",
    "Nonprofits & Associations",
    "Restaurants & Hospitality",
  ],
};

---

CHANGE 2: Update the nav links in client/src/components/header.tsx
Find the array of nav links and replace it with:

const navLinks = [
  { label: "Services", href: "/#services" },
  { label: "Industries", href: "/#industries" },
  { label: "Calculators", href: "/calculators" },
  { label: "AI for Firms", href: "/ai-consulting" },
  { label: "About", href: "/#about" },
  { label: "FAQ", href: "/#faq" },
  { label: "Blog", href: "/blog" },
];

---

CHANGE 3: Create a new file client/src/pages/calculators.tsx with this content:

import { Helmet } from "react-helmet";
import { Link } from "wouter";

const calculators = [
  {
    category: "Business Structure",
    tools: [
      {
        name: "S-Corp Tax Savings Calculator",
        desc: "Compare sole proprietor vs. S-Corp tax liability side by side. See your exact savings using 2025 federal rates.",
        href: "/calculators/scorp-savings",
        badge: "Most Popular",
      },
      {
        name: "Self-Employment Tax Calculator",
        desc: "Calculate your SE tax liability (15.3%) and the above-the-line deduction you can take on Form 1040.",
        href: "/calculators/self-employment-tax",
        badge: null,
      },
    ],
  },
  {
    category: "Tax Planning",
    tools: [
      {
        name: "Quarterly Estimated Tax Calculator",
        desc: "Calculate your Q1–Q4 federal and Maryland estimated tax payments. Avoid underpayment penalties.",
        href: "/calculators/estimated-tax",
        badge: null,
      },
      {
        name: "Home Office Deduction Calculator",
        desc: "Actual vs. simplified method comparison. See which saves more based on your home size and expenses.",
        href: "/calculators/home-office",
        badge: null,
      },
      {
        name: "Mileage Deduction Calculator",
        desc: "2025 standard mileage rate: $0.70/mile. Calculate your business mileage deduction for the year.",
        href: "/calculators/mileage",
        badge: null,
      },
      {
        name: "Section 179 Expensing Calculator",
        desc: "First-year equipment and asset deduction. See how much of your 2025 purchases you can deduct immediately.",
        href: "/calculators/section-179",
        badge: null,
      },
    ],
  },
  {
    category: "Business Finance",
    tools: [
      {
        name: "Fractional CFO ROI Calculator",
        desc: "See how quickly a fractional CFO engagement pays for itself based on your revenue and current pain points.",
        href: "/calculators/fractional-cfo-roi",
        badge: null,
      },
      {
        name: "Break-Even Analysis Calculator",
        desc: "How many units or client engagements do you need to cover your fixed costs? Know your number.",
        href: "/calculators/break-even",
        badge: null,
      },
      {
        name: "Cash Flow Forecast Calculator",
        desc: "Project your monthly cash position for the next 12 months based on revenue and expense assumptions.",
        href: "/calculators/cash-flow",
        badge: null,
      },
      {
        name: "Retirement Contribution Calculator",
        desc: "SEP-IRA, Solo 401(k), SIMPLE IRA — compare contribution limits and tax savings for self-employed.",
        href: "/calculators/retirement",
        badge: null,
      },
    ],
  },
];

export default function CalculatorsHub() {
  return (
    <>
      <Helmet>
        <title>Free Tax & Finance Calculators for Small Business | Selam CPA</title>
        <meta name="description" content="Free tax and financial calculators for small business owners — S-Corp savings, quarterly estimated tax, home office deduction, mileage, Section 179, and more. Built by Selam CPA, Maryland CPA firm." />
        <link rel="canonical" href="https://selamcpa.com/calculators" />
      </Helmet>

      <div className="bg-gray-50 border-b border-gray-200 py-3">
        <div className="max-w-6xl mx-auto px-6 md:px-12">
          <nav className="text-sm text-gray-500">
            <Link href="/"><span className="hover:text-emerald-600 cursor-pointer">Home</span></Link>
            <span className="mx-2">›</span>
            <span className="text-gray-900">Calculators</span>
          </nav>
        </div>
      </div>

      <div className="max-w-6xl mx-auto px-6 md:px-12 py-16">
        <div className="mb-12">
          <p className="text-emerald-600 text-sm font-medium tracking-widest uppercase mb-3">Free Tools · No Email Required</p>
          <h1 className="text-4xl md:text-5xl font-bold text-gray-900 mb-4">Tax & Finance Calculators</h1>
          <p className="text-xl text-gray-500 max-w-2xl leading-relaxed">
            Built for small business owners. Free to use. Real numbers based on 2025 federal and Maryland tax rates.
          </p>
        </div>

        <div className="space-y-12">
          {calculators.map((cat) => (
            <div key={cat.category}>
              <h2 className="text-lg font-bold text-gray-900 mb-4 pb-2 border-b border-gray-200">{cat.category}</h2>
              <div className="grid sm:grid-cols-2 gap-4">
                {cat.tools.map((tool) => (
                  <Link key={tool.name} href={tool.href}>
                    <div className="group border border-gray-200 rounded-xl p-6 hover:border-emerald-400 hover:shadow-md transition-all cursor-pointer h-full">
                      <div className="flex items-start justify-between mb-2">
                        <h3 className="font-semibold text-gray-900 group-hover:text-emerald-600 transition-colors">{tool.name}</h3>
                        {tool.badge && (
                          <span className="ml-2 flex-shrink-0 text-xs font-semibold text-emerald-700 bg-emerald-100 px-2 py-0.5 rounded-full">{tool.badge}</span>
                        )}
                      </div>
                      <p className="text-gray-500 text-sm leading-relaxed">{tool.desc}</p>
                    </div>
                  </Link>
                ))}
              </div>
            </div>
          ))}
        </div>

        <div className="mt-16 bg-[#0a0f1e] rounded-2xl p-10 text-center">
          <p className="text-emerald-400 text-sm font-medium tracking-widest uppercase mb-3">Need more than a calculator?</p>
          <h2 className="text-3xl font-bold text-white mb-4">Talk to a CPA who knows your numbers.</h2>
          <p className="text-gray-400 max-w-xl mx-auto mb-8">Calculators give you estimates. A CPA gives you a strategy. Book a free 30-minute call.</p>
          <a href="https://calendly.com/yber2001/30min" target="_blank" rel="noopener noreferrer" className="inline-flex items-center justify-center px-8 py-4 bg-emerald-500 hover:bg-emerald-400 text-white font-semibold rounded-lg transition-colors">
            Book a Free Discovery Call →
          </a>
        </div>
      </div>
    </>
  );
}

---

CHANGE 4: Create a new folder client/src/pages/calculators/ and inside it create a file scorp-savings.tsx with this content:

import { useState } from "react";
import { Helmet } from "react-helmet";
import { Link } from "wouter";

const fmt = (n: number) => n.toLocaleString("en-US", { style: "currency", currency: "USD", maximumFractionDigits: 0 });
const pct = (n: number) => `${(n * 100).toFixed(1)}%`;

const SS_WAGE_BASE = 176100;
const SS_RATE = 0.124;
const MEDICARE_RATE = 0.029;
const SE_RATE = 0.9235;

function calcSETax(profit: number) {
  const net = profit * SE_RATE;
  return Math.min(net, SS_WAGE_BASE) * (SS_RATE / 2 * 2) + net * MEDICARE_RATE;
}

function calcIncomeTax(taxable: number, status: string) {
  if (taxable <= 0) return 0;
  const brackets = status === "mfj"
    ? [[23200,0.1],[94300,0.12],[201050,0.22],[383900,0.24],[487450,0.32],[731200,0.35],[Infinity,0.37]]
    : [[11600,0.1],[47150,0.12],[100525,0.22],[191950,0.24],[243725,0.32],[609350,0.35],[Infinity,0.37]];
  let tax = 0, prev = 0;
  for (const [top, rate] of brackets) {
    if (taxable <= prev) break;
    tax += (Math.min(taxable, top as number) - prev) * (rate as number);
    prev = top as number;
  }
  return tax;
}

function Slider({ label, value, min, max, step, onChange, format, hint }: {
  label: string; value: number; min: number; max: number; step: number;
  onChange: (v: number) => void; format: (v: number) => string; hint?: string;
}) {
  return (
    <div className="mb-6">
      <div className="flex justify-between items-baseline mb-2">
        <label className="text-sm font-medium text-gray-700">{label}</label>
        <span className="text-lg font-bold text-emerald-600">{format(value)}</span>
      </div>
      <input type="range" min={min} max={max} step={step} value={value}
        onChange={(e) => onChange(Number(e.target.value))}
        className="w-full h-2 bg-gray-200 rounded-full appearance-none cursor-pointer accent-emerald-500" />
      <div className="flex justify-between text-xs text-gray-400 mt-1">
        <span>{format(min)}</span><span>{format(max)}</span>
      </div>
      {hint && <p className="text-xs text-gray-400 mt-1">{hint}</p>}
    </div>
  );
}

export default function ScorpSavingsCalculator() {
  const [profit, setProfit] = useState(120000);
  const [salary, setSalary] = useState(60000);
  const [status, setStatus] = useState("single");
  const [otherIncome, setOtherIncome] = useState(0);
  const [scorpCost, setScorpCost] = useState(2000);
  const [email, setEmail] = useState("");
  const [submitted, setSubmitted] = useState(false);
  const [showEmail, setShowEmail] = useState(false);

  const stdDed = status === "mfj" ? 30000 : 15000;

  // Sole prop
  const spSETax = calcSETax(profit);
  const spSEDed = spSETax / 2;
  const spQBI = Math.max(0, profit - spSEDed - stdDed) * 0.2;
  const spTaxable = Math.max(0, profit + otherIncome - spSEDed - stdDed - spQBI);
  const spIncomeTax = calcIncomeTax(spTaxable, status);
  const spTotal = spSETax + spIncomeTax;

  // S-Corp
  const sal = Math.min(salary, profit);
  const dist = Math.max(0, profit - sal);
  const empFICA = Math.min(sal, SS_WAGE_BASE) * (SS_RATE / 2) + sal * (MEDICARE_RATE / 2);
  const erFICA = empFICA;
  const scPayroll = empFICA + erFICA;
  const scQBI = dist * 0.2;
  const scGross = sal + dist + otherIncome - erFICA - scorpCost;
  const scTaxable = Math.max(0, scGross - stdDed - scQBI);
  const scIncomeTax = calcIncomeTax(scTaxable, status);
  const scTotal = scPayroll + scIncomeTax;

  const savings = spTotal - scTotal;
  const saving = savings > 0;

  const faqs = [
    { q: "How much do I need to earn before S-Corp election makes sense?", a: "Most CPAs recommend at least $60,000–$80,000 in net self-employment profit. Below that threshold, payroll processing fees, state filing fees, and the cost of Form 1120-S preparation often exceed the tax savings." },
    { q: "What is a reasonable salary for an S-Corp owner in Maryland?", a: "The IRS requires compensation comparable to what an arm's-length employer would pay. For Maryland consultants, this typically ranges $50,000–$90,000 depending on industry. The IRS actively audits S-Corps where salary is unusually low relative to distributions." },
    { q: "Do I need to form a new entity to elect S-Corp status?", a: "No. An existing LLC can elect S-Corp tax treatment by filing IRS Form 2553. The LLC continues as a legal entity — only the tax treatment changes. The election must generally be made by March 15 of the tax year you want it to take effect." },
    { q: "Does Maryland have its own S-Corp tax rules?", a: "Yes. Maryland follows federal S-Corp treatment but imposes its own rates (2%–5.75%) plus county tax (2.25%–3.2%). Maryland also requires S-Corps to file Form 510. State tax planning is as important as federal for Maryland-based S-Corp owners." },
    { q: "Can I elect S-Corp status for an LLC in Maryland?", a: "Yes. A Maryland LLC can elect S-Corp taxation by filing IRS Form 2553. The LLC remains a Maryland LLC for legal purposes — only the federal and state tax treatment changes." },
  ];

  return (
    <>
      <Helmet>
        <title>S-Corp Tax Savings Calculator 2025 | Selam CPA — Laurel, Maryland</title>
        <meta name="description" content="Free S-Corp tax savings calculator for 2025. Compare sole proprietor vs. S-Corp taxes side by side. Built by Selam CPA, Maryland CPA firm serving DMV and all 50 states." />
        <meta name="keywords" content="S-Corp tax savings calculator 2025, S-Corp election Maryland, self-employment tax savings, reasonable salary S-Corp, LLC vs S-Corp tax" />
        <link rel="canonical" href="https://selamcpa.com/calculators/scorp-savings" />
        <script type="application/ld+json">{JSON.stringify({
          "@context": "https://schema.org",
          "@type": "FAQPage",
          mainEntity: faqs.map(f => ({ "@type": "Question", name: f.q, acceptedAnswer: { "@type": "Answer", text: f.a } }))
        })}</script>
      </Helmet>

      <div className="bg-gray-50 border-b border-gray-200 py-3">
        <div className="max-w-6xl mx-auto px-6 md:px-12">
          <nav className="text-sm text-gray-500">
            <Link href="/"><span className="hover:text-emerald-600 cursor-pointer">Home</span></Link>
            <span className="mx-2">›</span>
            <Link href="/calculators"><span className="hover:text-emerald-600 cursor-pointer">Calculators</span></Link>
            <span className="mx-2">›</span>
            <span className="text-gray-900">S-Corp Tax Savings</span>
          </nav>
        </div>
      </div>

      <div className="max-w-6xl mx-auto px-6 md:px-12 py-16">
        <div className="mb-12">
          <p className="text-emerald-600 text-sm font-medium tracking-widest uppercase mb-3">Free Calculator · 2025 Tax Year</p>
          <h1 className="text-4xl md:text-5xl font-bold text-gray-900 mb-4">S-Corp Tax Savings Calculator</h1>
          <p className="text-xl text-gray-500 max-w-2xl leading-relaxed">
            See exactly how much you could save by electing S-Corp status — sole proprietor vs. S-Corp, side by side, using 2025 rates.
          </p>
          <p className="text-sm text-gray-400 mt-3">For estimation purposes only. <a href="/tax" className="text-emerald-600 hover:underline">Consult a CPA before making the S-Corp election →</a></p>
        </div>

        <div className="grid lg:grid-cols-5 gap-10">
          <div className="lg:col-span-2">
            <div className="bg-white border border-gray-200 rounded-2xl p-6 sticky top-6">
              <h2 className="text-lg font-bold text-gray-900 mb-6">Your Numbers</h2>
              <div className="mb-6">
                <label className="text-sm font-medium text-gray-700 block mb-2">Filing Status</label>
                <div className="flex gap-2">
                  {[{ value: "single", label: "Single / HOH" }, { value: "mfj", label: "Married Filing Jointly" }].map(opt => (
                    <button key={opt.value} onClick={() => setStatus(opt.value)}
                      className={`flex-1 py-2 px-3 rounded-lg text-sm font-medium border transition-colors ${status === opt.value ? "bg-emerald-500 border-emerald-500 text-white" : "bg-white border-gray-200 text-gray-600 hover:border-emerald-400"}`}>
                      {opt.label}
                    </button>
                  ))}
                </div>
              </div>
              <Slider label="Net Business Profit" value={profit} min={30000} max={500000} step={5000} onChange={setProfit} format={fmt} hint="Total business income minus expenses" />
              <Slider label="Reasonable Salary (S-Corp)" value={salary} min={20000} max={Math.min(profit, 200000)} step={5000} onChange={setSalary} format={fmt} hint="IRS scrutinizes unusually low salaries" />
              <Slider label="Other Income" value={otherIncome} min={0} max={200000} step={5000} onChange={setOtherIncome} format={fmt} hint="Spouse W-2, rental income, etc." />
              <Slider label="Annual S-Corp Maintenance" value={scorpCost} min={500} max={8000} step={250} onChange={setScorpCost} format={fmt} hint="Payroll + Form 1120-S + state fees. Typical: $1,500–$3,000" />
              <a href="https://calendly.com/yber2001/30min" target="_blank" rel="noopener noreferrer"
                className="block w-full text-center py-3 bg-emerald-500 hover:bg-emerald-400 text-white font-semibold rounded-lg transition-colors mt-4">
                Discuss With a CPA →
              </a>
            </div>
          </div>

          <div className="lg:col-span-3 space-y-6">
            <div className={`rounded-2xl p-8 ${saving ? "bg-emerald-500 text-white" : "bg-gray-100 text-gray-700"}`}>
              {saving ? (
                <>
                  <p className="text-emerald-100 text-sm font-medium tracking-widest uppercase mb-2">Estimated Annual Tax Savings</p>
                  <p className="text-6xl font-bold mb-2">{fmt(savings)}</p>
                  <p className="text-emerald-100 text-lg">{fmt(savings * 10)} over 10 years · {pct((spTotal - scTotal) / profit)} lower effective rate</p>
                </>
              ) : (
                <>
                  <p className="text-gray-500 text-sm font-medium tracking-widest uppercase mb-2">Result</p>
                  <p className="text-3xl font-bold mb-2">S-Corp May Not Save Money Yet</p>
                  <p className="text-gray-500">At this profit level, S-Corp costs exceed tax savings. The election typically makes sense above $60,000–$80,000 net profit.</p>
                </>
              )}
            </div>

            <div className="grid grid-cols-2 gap-4">
              <div className="bg-white border border-gray-200 rounded-2xl p-6">
                <p className="text-xs font-medium text-gray-400 tracking-widest uppercase mb-4">Sole Proprietor / LLC</p>
                <div className="space-y-3 text-sm">
                  <div className="flex justify-between"><span className="text-gray-500">SE tax</span><span className="font-medium text-red-500">{fmt(spSETax)}</span></div>
                  <div className="flex justify-between"><span className="text-gray-500">SE deduction</span><span className="font-medium text-gray-700">-{fmt(spSEDed)}</span></div>
                  <div className="flex justify-between"><span className="text-gray-500">QBI deduction</span><span className="font-medium text-gray-700">-{fmt(spQBI)}</span></div>
                  <div className="flex justify-between"><span className="text-gray-500">Income tax</span><span className="font-medium text-red-400">{fmt(spIncomeTax)}</span></div>
                  <div className="border-t border-gray-100 pt-3 flex justify-between">
                    <span className="font-semibold text-gray-900">Total tax</span>
                    <span className="font-bold text-gray-900">{fmt(spTotal)}</span>
                  </div>
                  <div className="flex justify-between"><span className="text-gray-400">Effective rate</span><span className="text-gray-600">{pct(spTotal / profit)}</span></div>
                </div>
              </div>

              <div className={`rounded-2xl p-6 border-2 ${saving ? "bg-emerald-50 border-emerald-300" : "bg-white border-gray-200"}`}>
                {saving && <span className="inline-block text-xs font-semibold text-emerald-700 bg-emerald-200 px-2 py-0.5 rounded-full mb-3">Lower Tax</span>}
                <p className="text-xs font-medium text-gray-400 tracking-widest uppercase mb-4">S-Corporation</p>
                <div className="space-y-3 text-sm">
                  <div className="flex justify-between"><span className="text-gray-500">Payroll tax (FICA)</span><span className="font-medium text-amber-600">{fmt(scPayroll)}</span></div>
                  <div className="flex justify-between"><span className="text-gray-500">Employer FICA deduction</span><span className="font-medium text-gray-700">-{fmt(erFICA)}</span></div>
                  <div className="flex justify-between"><span className="text-gray-500">QBI deduction</span><span className="font-medium text-gray-700">-{fmt(scQBI)}</span></div>
                  <div className="flex justify-between"><span className="text-gray-500">S-Corp costs</span><span className="font-medium text-gray-500">-{fmt(scorpCost)}</span></div>
                  <div className="flex justify-between"><span className="text-gray-500">Income tax</span><span className="font-medium text-amber-500">{fmt(scIncomeTax)}</span></div>
                  <div className="border-t border-gray-200 pt-3 flex justify-between">
                    <span className="font-semibold text-gray-900">Total tax</span>
                    <span className={`font-bold ${saving ? "text-emerald-700" : "text-gray-900"}`}>{fmt(scTotal)}</span>
                  </div>
                  <div className="flex justify-between"><span className="text-gray-400">Effective rate</span><span className={saving ? "text-emerald-700 font-medium" : "text-gray-600"}>{pct(scTotal / profit)}</span></div>
                </div>
              </div>
            </div>

            {saving && (
              <div className="bg-[#0a0f1e] rounded-2xl p-6">
                <p className="text-emerald-400 text-xs font-medium tracking-widest uppercase mb-3">What This Means</p>
                <p className="text-white leading-relaxed">
                  By paying yourself a salary of {fmt(sal)} and taking {fmt(dist)} as a distribution,
                  you avoid self-employment tax on the distribution. After S-Corp maintenance costs,
                  your net annual savings are <strong className="text-emerald-400">{fmt(savings)}</strong> — or <strong className="text-emerald-400">{fmt(savings * 10)}</strong> over 10 years.
                </p>
                <p className="text-gray-400 text-sm mt-3">Estimates use 2025 federal rates only. Maryland state and county taxes will affect your actual savings.</p>
              </div>
            )}

            {saving && !submitted && (
              <div className="border border-emerald-200 bg-emerald-50 rounded-2xl p-6">
                <h3 className="font-bold text-gray-900 mb-2">Get a personalized S-Corp analysis</h3>
                <p className="text-gray-500 text-sm mb-4">We'll send you a summary of these results plus a checklist for making the S-Corp election in Maryland.</p>
                {!showEmail ? (
                  <button onClick={() => setShowEmail(true)} className="w-full py-3 bg-emerald-500 hover:bg-emerald-400 text-white font-semibold rounded-lg transition-colors">
                    Send Me the Analysis →
                  </button>
                ) : (
                  <div className="flex gap-2">
                    <input type="email" placeholder="you@example.com" value={email} onChange={e => setEmail(e.target.value)}
                      className="flex-1 border border-gray-200 rounded-lg px-4 py-2 text-sm focus:outline-none focus:border-emerald-400" />
                    <button onClick={() => { if (email) setSubmitted(true); }}
                      className="px-6 py-2 bg-emerald-500 hover:bg-emerald-400 text-white font-semibold rounded-lg text-sm">Send</button>
                  </div>
                )}
              </div>
            )}

            {submitted && (
              <div className="border border-emerald-200 bg-emerald-50 rounded-2xl p-6 text-center">
                <p className="text-emerald-700 font-semibold text-lg mb-1">Thank you!</p>
                <p className="text-gray-500 text-sm">We'll send your analysis to {email} within one business day.</p>
              </div>
            )}
          </div>
        </div>

        <div className="mt-20">
          <h2 className="text-3xl font-bold text-gray-900 mb-8">S-Corp Election — Common Questions</h2>
          <div className="space-y-4 max-w-3xl">
            {faqs.map((faq, i) => (
              <details key={i} className="border border-gray-200 rounded-xl group">
                <summary className="flex justify-between items-center p-5 cursor-pointer font-semibold text-gray-900 hover:text-emerald-600 transition-colors list-none">
                  {faq.q}
                  <span className="text-emerald-500 ml-4 flex-shrink-0 group-open:rotate-45 transition-transform text-xl">+</span>
                </summary>
                <div className="px-5 pb-5"><p className="text-gray-500 leading-relaxed">{faq.a}</p></div>
              </details>
            ))}
          </div>
        </div>

        <div className="mt-16 pt-12 border-t border-gray-200">
          <h2 className="text-2xl font-bold text-gray-900 mb-6">Related Calculators</h2>
          <div className="grid sm:grid-cols-3 gap-4">
            {[
              { name: "Quarterly Estimated Tax", desc: "Calculate Q1–Q4 federal and Maryland estimated payments", href: "/calculators/estimated-tax" },
              { name: "Self-Employment Tax", desc: "Calculate SE tax liability and the deductible portion", href: "/calculators/self-employment-tax" },
              { name: "Home Office Deduction", desc: "Actual vs. simplified method — which saves more?", href: "/calculators/home-office" },
            ].map(calc => (
              <Link key={calc.name} href={calc.href}>
                <div className="border border-gray-200 rounded-xl p-5 hover:border-emerald-400 hover:shadow-md transition-all cursor-pointer group">
                  <h3 className="font-semibold text-gray-900 mb-1 group-hover:text-emerald-600 transition-colors">{calc.name}</h3>
                  <p className="text-gray-400 text-sm">{calc.desc}</p>
                </div>
              </Link>
            ))}
          </div>
        </div>

        <div className="mt-16 bg-[#0a0f1e] rounded-2xl p-10 text-center">
          <p className="text-emerald-400 text-sm font-medium tracking-widest uppercase mb-3">Next Step</p>
          <h2 className="text-3xl font-bold text-white mb-4">Ready to make the S-Corp election?</h2>
          <p className="text-gray-400 max-w-xl mx-auto mb-8">We handle the S-Corp election, payroll setup, and ongoing compliance for Maryland small businesses. Book a free call.</p>
          <a href="https://calendly.com/yber2001/30min" target="_blank" rel="noopener noreferrer"
            className="inline-flex items-center justify-center px-8 py-4 bg-emerald-500 hover:bg-emerald-400 text-white font-semibold rounded-lg transition-colors">
            Book a Free Discovery Call →
          </a>
          <p className="text-gray-500 text-sm mt-4">Serving Laurel MD · Columbia MD · Baltimore · DC · Northern Virginia · All 50 States</p>
        </div>
      </div>
    </>
  );
}

---

CHANGE 5: Add these routes to client/src/App.tsx inside the Router component, alongside the existing routes:

<Route path="/calculators" component={lazy(() => import("./pages/calculators"))} />
<Route path="/calculators/scorp-savings" component={lazy(() => import("./pages/calculators/scorp-savings"))} />

Make sure lazy is imported from React at the top of App.tsx if it is not already.

---

Please implement all 5 changes, keeping all existing code intact. Do not remove any existing routes, components, or functionality. Only add and update what is specified above.